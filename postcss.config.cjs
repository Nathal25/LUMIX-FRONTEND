module.exports = {
  plugins: {
    // Tailwind v4 splits the PostCSS plugin into a separate package. Use
    // `@tailwindcss/postcss` here (it's present in devDependencies).
    '@tailwindcss/postcss': {},
    autoprefixer: {},
  }
}
